"""Heritage Lens FastAPI layer.

A thin REST wrapper around the existing agent pipeline. Nothing here
reimplements pipeline logic — it imports agent.* and shapes the output for
the React frontend.
"""
